<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>R.C. ID | DIGITAL TAG</title>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@700&family=Rajdhani:wght@500;700&display=swap" rel="stylesheet">
    
    <style>
        :root { 
            --accent: #FF003C; 
            --bg-dark: #050505; 
            --glass: rgba(20, 20, 20, 0.85); 
            --border: rgba(255, 255, 255, 0.15);
            --text-main: #EAEAEA;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; outline: none !important; -webkit-tap-highlight-color: transparent; }
        body { background-color: var(--bg-dark); color: var(--text-main); font-family: 'Rajdhani', sans-serif; height: 100vh; overflow: hidden; display: flex; justify-content: center; align-items: center; }

        /* --- THE CARD CONTAINER --- */
        .id-card {
            width: 100%;
            max-width: 400px;
            height: 100vh;
            background: radial-gradient(circle at top right, #1a1a1a, #000);
            position: relative;
            overflow-y: auto; /* Allow scrolling inside */
            padding: 30px 20px;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        /* --- PROFILE HEADER --- */
        .profile-header {
            text-align: center;
            margin-bottom: 20px;
            position: relative;
        }

        .avatar-glow {
            width: 120px; height: 120px;
            margin: 0 auto 15px;
            border-radius: 50%;
            padding: 3px;
            background: linear-gradient(45deg, var(--accent), #111);
            position: relative;
        }

        .avatar {
            width: 100%; height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #000;
            background: #222;
        }

        .status-dot {
            width: 15px; height: 15px; background: #00FF88;
            border-radius: 50%;
            position: absolute; bottom: 5px; right: 5px;
            border: 2px solid #000;
            box-shadow: 0 0 10px #00FF88;
        }

        h1 { font-family: 'Oswald'; font-size: 2rem; text-transform: uppercase; line-height: 1; margin-bottom: 5px; }
        .role { color: var(--accent); font-weight: 700; letter-spacing: 2px; font-size: 0.9rem; text-transform: uppercase; }
        .bio { color: #888; font-size: 0.9rem; margin-top: 10px; line-height: 1.4; max-width: 80%; margin-left: auto; margin-right: auto; }

        /* --- LINK BLOCKS (The "Bento" Links) --- */
        .link-grid { display: flex; flex-direction: column; gap: 12px; }

        .link-btn {
            background: var(--glass);
            border: 1px solid var(--border);
            padding: 18px 20px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            text-decoration: none;
            color: white;
            font-family: 'Oswald';
            font-size: 1.1rem;
            letter-spacing: 1px;
            transition: 0.2s;
            backdrop-filter: blur(10px);
        }

        .link-btn:hover {
            background: var(--accent);
            border-color: var(--accent);
            transform: translateX(5px);
            color: black;
        }

        .link-icon { width: 30px; font-size: 1.2rem; text-align: center; margin-right: 15px; }
        .arrow { margin-left: auto; font-size: 0.8rem; opacity: 0.5; }

        /* --- SPECIAL FEATURE: SAVE CONTACT --- */
        .save-btn {
            background: white; color: black;
            text-align: center; padding: 20px;
            border-radius: 50px;
            font-family: 'Oswald'; font-size: 1.2rem;
            text-decoration: none; display: block;
            margin-top: 20px;
            box-shadow: 0 0 20px rgba(255,255,255,0.2);
        }

        /* --- BRANDING --- */
        .watermark {
            text-align: center; margin-top: 40px; opacity: 0.3;
            font-size: 0.7rem; letter-spacing: 2px;
        }

    </style>
</head>
<body>

    <div class="id-card">
        
        <div class="profile-header">
            <div class="avatar-glow">
                <img src="https://via.placeholder.com/150" class="avatar" alt="Profile">
                <div class="status-dot"></div>
            </div>
            <h1 id="user-name">AZHARI</h1>
            <p class="role">REBELIVIOUS CREATIVE</p>
            <p class="bio">Graffiti Artist. 3D Designer. Digital Architect based in Johor Bahru.</p>
        </div>

        <div class="link-grid">
            
            <a href="#" class="link-btn">
                <i class="fab fa-whatsapp link-icon"></i>
                WHATSAPP
                <i class="fas fa-chevron-right arrow"></i>
            </a>

            <a href="#" class="link-btn">
                <i class="fab fa-instagram link-icon"></i>
                INSTAGRAM
                <i class="fas fa-chevron-right arrow"></i>
            </a>

            <a href="#" class="link-btn">
                <i class="fas fa-cube link-icon"></i>
                3D PORTFOLIO
                <i class="fas fa-chevron-right arrow"></i>
            </a>

            <a href="#" class="link-btn">
                <i class="fas fa-map-marker-alt link-icon"></i>
                LOCATE STUDIO
                <i class="fas fa-chevron-right arrow"></i>
            </a>

        </div>

        <a href="#" class="save-btn">SAVE CONTACT TO PHONE</a>

        <div class="watermark">POWERED BY R.C. ID</div>

    </div>

</body>
</html>